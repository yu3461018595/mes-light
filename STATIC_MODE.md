# 静态版（浏览器本地数据）说明

本目录 `public/` 是一套**纯前端**轻量 MES，可直接托管到 CloudStudio / 任意静态空间，
无需 Node 后端、无需数据库。适用于演示、单机试用，或作为「动态版」上线前的预览。

## 工作原理
- `js/store.js`：在浏览器内用纯 JS 复刻了后端的全部接口（登录 / 工单 / 报工 / 看板 /
  基础数据 / 扫码 / 统计 / 日志 / 二维码）。数据持久化在 `localStorage`，首次打开
  从 `data/seed.json` 初始化（报工日期会自动锚定到「今天」，看板不会空）。
- `js/api.js`：**自动探测**。打开页面时先请求 `/api/meta`：
  - 命中真实后端 → 走 `fetch('/api/...')`（动态模式）；
  - 未命中（静态托管）→ 切换为 `Store.handle(...)`（本地模式）。
  两套模式下返回结构完全一致，视图层代码 zero-change。
- `lib/qrcode.js`：前端二维码库，扫码报工的二维码在浏览器内直接生成（内联 SVG）。

## 使用方法
1. 打开首页 → 用演示账号登录：
   - `admin` / `123456`（管理员，可改工单、派工、看基础数据）
   - `leader1` / `123456`（班组长）
   - `worker1` / `123456`（操作工，默认进报工页）
2. 看板、工单、报工、基础数据、报表、日志全部可用；新增/报工/删除都会写回
   `localStorage`，刷新不丢。
3. 「工单二维码 / 员工二维码」弹窗里的链接用微信扫一扫，或在微信里直接打开
   `…/m/index.html?o=<工单ID>&t=static` 即可进入手机报工页。

## 重要限制（务必了解）
- **数据是浏览器本地的**：不同浏览器、不同设备、无痕模式之间**不共享**。
  也就是说，静态版是「单机/单浏览器演示」，并非真正的多人协同。
- 想要**真实多人在线、数据集中、跨设备共享**，请切换到动态版（见下）。

## 如何切换为「动态版」（真实后端）
无需改动任何前端代码，`api.js` 会自动探测：
1. 启动已有的 Node 后端：`node server.js`（读取环境变量 `PORT`、绑定 `0.0.0.0`）。
2. 用同一个 `public/` 目录作为静态资源（后端同时托管 `/` 和 `/m/`，并处理 `/api/*`）。
3. 打开页面时 `api.js` 探测到 `/api/meta` 存在 → 自动切到动态模式，数据改为落库到
   SQLite，所有设备/账号实时共享。
4. 部署后端可用根目录已有的：`Dockerfile` / `railway.json` / `render.yaml` /
   `Procfile`（或任意支持 Node 的平台）。详见 `DEPLOY.md`。

## 重置数据
清空浏览器对该站点的 `localStorage`（key 为 `mes_static_v1`），或换无痕窗口，
即可恢复到种子数据初始状态。

## 回归测试
`test_static.cjs`（Node 运行）对 `store.js` 做冒烟测试：登录、会话恢复、工单聚合、
报工与撤销、统计、二维码链接、免登录 H5 接口、引用保护删除、工单增删。
运行：`node test_static.cjs`
