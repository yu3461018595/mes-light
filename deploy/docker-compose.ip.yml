# 阶段一（暂无域名 / 未备案）：把容器内 3000 直接映射到公网 8080，用 http://IP:8080 访问
#
# 用法：
#   docker compose -f docker-compose.yml -f deploy/docker-compose.ip.yml up -d
#
# 备案完成、改用 Nginx + HTTPS 后，请只用主文件（仅监听 127.0.0.1，由 Nginx 反代）：
#   docker compose up -d
services:
  mes:
    ports:
      - "0.0.0.0:8080:3000"
